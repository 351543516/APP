// lib/config/app_colors.dart
import 'package:flutter/material.dart'; // 确保导入了 Material 库（如果使用 Colors）

class AppColors {
  static const primaryColor = Color(0xFF2B3D86);
  static const accentColor = Color(0xFF6699CC);
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF6699CC);
  static const backgroundColor = Color(0xFF003366);
  static const featureCardColor = Color(0xFF003366);
  static const refinedGold = Color(0xFF6699CC);
  static const bottomNavBarBackgroundColor = Color(0xFF00264D);
}